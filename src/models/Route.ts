export enum AuthenticatedRoutePath {
	home = '/',
	board = 'board',
	report = 'report',
	settings = 'settings',
	logout = 'logout',
}

export enum UnAuthenticatedRoutePath {
	signin = 'signin',
	signup = 'signup',
}
